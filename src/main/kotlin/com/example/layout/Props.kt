package com.example.layout

enum class Size {
    WRAP_CONTENT, MATH_PARENT, HALF_PARENT
}
enum class Align {
    LEFT, RIGHT, CENTER, TOP, BOTTOM, CENTER_HORIZONTAL, CENTER_VERTICAL
}
enum class Table {
    ROOT, CELL, ROW, GRID
}
enum class Role {
    BUTTON, TABLE, ROW, CELL
}
